# Build artifacts
/bin/
/dist/

# Frontend deps + build output. The embedded SPA is rebuilt by `make frontend`
# (and in CI before the binary), so it is never committed.
/frontend/dist/
/frontend/node_modules/
/frontend/.svelte-kit/

# Local dev state
/dev/**
*.db

# OS / editor
.DS_Store
*.swp

# Test/tooling artifacts
.playwright-mcp/
